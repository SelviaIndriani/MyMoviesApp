<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.slv.mysubmissionandroidexpert">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".presentation.di.MainApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MySubmissionAndroidExpert">
        <activity android:name=".presentation.setting.SettingActivity" />
        <activity
            android:name=".presentation.detail.DetailMovieActivity"
            android:theme="@style/Theme.MySubmissionAndroidExpert.NoActionBar" />
        <activity android:name=".presentation.MainActivity" />
        <activity
            android:name=".presentation.splashscreen.SplashscreenActivity"
            android:theme="@style/Theme.MySubmissionAndroidExpert.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>